---
tests:
  - name: default
    template: config.yaml.j2
    expected_result: default/config.yaml

  - name: DHCP
    template: config.yaml.j2
    variables:
      inventory: dhcp/inventory.yml
    expected_result: dhcp/config.yaml

  - name: static
    template: config.yaml.j2
    variables:
      inventory: static/inventory.yml
    expected_result: static/config.yaml
